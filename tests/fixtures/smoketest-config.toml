[segmenter]

    [segmenter.model]
    depth = 2
    n_heads = 2

    [segmenter.lexer]
    transformer_model = "distilbert-base-multilingual-cased"
    embedding_dim = 100

[training]

    [training.run]
    max_epochs = 2

    [training.hparams]
    batch_size = 8
